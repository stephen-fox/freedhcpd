# capsicum

- Investigate reducing /dev/null capabilities, currently seems to
  allow everything
- Investigate reducing /var/run/log capabilities, currently seems
  to allow everything
- Fix sending of ICMP packets (may not be broken, may have been
  firewall policy stuff)
- Fix synchronization using multicast
